<!-- Static Field for id kelas -->
<div class="form-group">
    <div class="form-label">Ruangan Kelas</div>
    <div>{{ $class_schedules->classroom_id }}</div>
</div>

<!-- Static Field for day -->
<div class="form-group">
    <div class='form-label'>Day</div>
    <div>{{ $class_schedules->day }}</div>
</div>

<!-- Static Field for start at -->
<div class="form-group">
    <div class='form-label'>Mulai</div>
    <div>{{ $class_schedules->start_at }}</div>
</div>

<!-- Static Field for end at -->
<div class="form-group">
    <div class='form-label'>Berakhir</div>
    <div>{{ $class_schedules->end_at}}</div>
</div>

<!-- Static Field for periode -->
<div class="form-group">
    <div class='form-label'>Periode</div>
    <div>{{ $class_schedules->period}}</div>
</div>

<!-- Static Field for created at -->
<div class="form-group">
    <div class='form-label'>Dibuat di</div>
    <div>{{ $class_schedules->created_at}}</div>
</div>

<!-- Static Field for update at -->
<div class="form-group">
    <div class='form-label'>Diperbaharui di</div>
    <div>{{ $class_schedules->update_at}}</div>
</div>

<!-- Static Field for room_id -->
<div class="form-group">
    <div class='form-label'>Ruangan</div>
    <div>{{ $class_schedules->update_at}}</div>
</div>
